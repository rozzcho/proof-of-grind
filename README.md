# Proof of Grind

Solana Colosseum — Discord study group with USDC stake pools.

- `programs/proof_of_grind` — Anchor program (Rust)
- `app` — React (Vite) frontend

## Run

```bash
npm --prefix app run dev
```

Discord link: edit `DISCORD_INVITE_URL` in `app/src/config.ts`.
